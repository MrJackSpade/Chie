@using Llama.Data.Interfaces;
@using Llama.Data.Models;

@model IReadOnlyLlamaTokenCollection

@if (Model != null)
{
    foreach (LlamaToken token in Model)
    {
        @Html.Raw(token.Value?.Replace("\n", "<br />") ?? string.Empty)
    }
}