@using Llama.Data.Models;

@model IAsyncEnumerable<LlamaToken>

@if (Model != null)
{
    await foreach (LlamaToken token in Model)
    {
        if (token.Id == 13)
        {
            <br />
        } else
        {
            @token.ToString()
        }
    }
}